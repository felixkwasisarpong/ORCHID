version: "3.9"
services:
  mcp:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: ["uvicorn", "tools.synthetic_mcp_server.app:app", "--host", "0.0.0.0", "--port", "9000"]
    ports:
      - "9000:9000"
  orchestrator:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: ["uvicorn", "api.app:app", "--host", "0.0.0.0", "--port", "8000"]
    environment:
      MCP_BASE_URL: "http://mcp:9000"
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      - mcp
